@page
@model Frontend.Pages.Nutricionistas.CreateModel

@{
    ViewData["Title"] = "Registrar Nuevo Nutricionista";
}

<h1>Registrar Nuevo Nutricionista</h1>

<a asp-page="Index" class="btn btn-secondary mb-3">Volver al listado</a>

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var entry in ViewData.ModelState.Values)
            {
                @foreach (var error in entry.Errors)
                {
                    <li>@error.ErrorMessage</li>
                }
            }
        </ul>
    </div>
}

<form method="post" class="needs-validation" novalidate>
    <div class="form-group mb-3">
        <label asp-for="Nutricionista.IdPersona" class="form-label">Persona</label>
        <select asp-for="Nutricionista.IdPersona" class="form-control" required>
            <option value="">-- Seleccione una Persona --</option>
            @if (Model.Personas != null)
            {
                @foreach (var persona in Model.Personas)
                {
                    <option value="@persona.IdPersona">
                        @persona.Nombre @persona.ApellidoPaterno @persona.ApellidoMaterno - CI: @persona.CI
                    </option>
                }
            }
        </select>
        <span asp-validation-for="Nutricionista.IdPersona" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="Nutricionista.Matricula" class="form-label"></label>
        <input asp-for="Nutricionista.Matricula" class="form-control" required />
        <span asp-validation-for="Nutricionista.Matricula" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="Nutricionista.Especialidad" class="form-label"></label>
        <input asp-for="Nutricionista.Especialidad" class="form-control" />
        <span asp-validation-for="Nutricionista.Especialidad" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="Nutricionista.AnosExperiencia" class="form-label"></label>
        <input asp-for="Nutricionista.AnosExperiencia" type="number" class="form-control" />
        <span asp-validation-for="Nutricionista.AnosExperiencia" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Registrar</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


